# Registro de deuda técnica — Incremento 02 (Cotizador)

> Formato heredado del Incremento 01 (LA-2026-020): P0 = crítico, P3 = bajo. Cubre ambos repos del incremento (`rutacar_landing_back` y el proxy en `rutacar_landing_new`).

| ID | Ítem | Prioridad | Repo | Descripción | Acción antes de DP-FINAL |
|---|---|---|---|---|---|
| DT-01 | Reglas de cálculo provisionales | P0 | backend | `src/services/pricing.service.ts` usa un rango conservador por tipología (`v0-conservador-provisional`), no las reglas reales del negocio. | Bloqueante real de DP-FINAL — depende de la reunión con el equipo comercial de Ruta Car (riesgo R-001). |
| DT-02 | Notificación por email conectada a Resend, pero con dominio de pruebas — **ambos destinos fallan en producción hoy**, no solo el del cliente | P0 | backend | `notification.service.ts`/`email.ts` ya usan el SDK de Resend, no un stub. `FROM_EMAIL=onboarding@resend.dev` es el dominio de pruebas compartido de Resend: en este modo Resend solo entrega hacia el email dueño de la cuenta (`kris.sandoval010@gmail.com`), no hacia cualquier `ADMIN_EMAIL` que se configure. **Verificado en producción el 2026-07-06** (envío real de prueba vía `/api/precalificaciones`, log de Railway): `ADMIN_EMAIL=ksandoval@cerveros.cl` ≠ email dueño de la cuenta Resend → 403 también en `notificarEquipoComercial`, no solo en `confirmarAlCliente` como se creía antes. El lead sí se persiste (201, R-004 — no falla en silencio, `notificacionIntentos` queda registrado), pero **ningún email de notificación llega a nadie hoy**, ni al cliente ni al equipo comercial. | Bloqueante real de DP-FINAL, ahora P0: (a) fix inmediato de bajo costo — actualizar `ADMIN_EMAIL` en Railway a `kris.sandoval010@gmail.com` para que `notificarEquipoComercial` vuelva a entregar mientras se resuelve lo de fondo; (b) fix real — verificar un dominio propio de Ruta Car en resend.com/domains y actualizar `FROM_EMAIL`. |
| DT-03 | Verificado y cerrado: backend desplegado en Railway | Cerrado | backend | MongoDB Atlas provisionado (`clusteramerico`, base de datos propia `rutacar_api`) y servicio `rutacar_landing_back` creado en Railway (proyecto `6a22f115-555d-40fe-baf7-4d0c0f6cc32f`). | Ninguna — deploy confirmado `SUCCESS`, conectado a Mongo (ver DT-09 por el incidente de `targetPort` detectado en esta misma verificación). |
| DT-04 | WhatsApp Business API no conectado | P2 | backend + frontend | La confirmación al cliente y la notificación al equipo solo usan email. WhatsApp queda `[POR DEFINIR CON RUTA CAR]` (requiere cuenta Meta verificada). | Evaluar cuando Ruta Car tenga la cuenta lista; no bloquea el MVP (el click-to-chat del Incremento 01 cubre el canal manual). |
| DT-05 | Verificado y cerrado: formulario multi-paso | Cerrado | frontend | Construido en `app/cotizador/` + `components/cotizador/` (WBS 2.1.3, Sprint 1) — verificado en navegador con Playwright: las 8 tipologías, validación por paso, envío feliz, escenarios de error simulados y precarga desde la ficha de tipología. | Ninguna — prueba end-to-end contra el backend real completada, local y en producción (ver DT-10). |
| DT-06 | Verificado y cerrado: `NEXT_PUBLIC_USE_MOCKS` | Cerrado | frontend | Implementado en `lib/cotizador/api.ts`/`mocks.ts` junto con el formulario multi-paso (LA-2026-010). | Ninguna. |
| DT-07 | Contrato de tipos entre los dos repos mantenido a mano | P2 | ambos | No hay un paquete de tipos compartido — `CrearCotizacionInput` (backend) y el payload que arma el proxy (frontend) se mantienen sincronizados manualmente. | Si el número de endpoints crece (Incrementos 04/07), evaluar un paquete compartido (ver R-008 del prompt maestro). |
| DT-08 | Verificado y corregido: slugs de tipología en `pricing.service.ts` | Cerrado | backend | La primera versión usaba slugs inventados que no coincidían con el catálogo real (`lib/content/tipologias.ts`). Detectado y corregido durante la verificación end-to-end del proxy. | Ninguna — cerrado, ver commit "Corregir slugs de tipologia...". Se deja registrado como lección: sin la prueba end-to-end real, este bug habría llegado a producción silenciosamente (ningún test unitario lo detectaba, porque el backend no valida sus reglas contra el catálogo del otro repo). |
| DT-09 | Verificado y corregido: `targetPort` desalineado en el dominio de Railway | Cerrado | backend | Al crear el servicio `rutacar_landing_back` en Railway, el dominio público quedó con `targetPort` fijo en 4000 (el default de `.env.example`), pero Railpack le asignó a Express el puerto 8080 en runtime — `GET /health` devolvía 502. Mismo patrón de incidente que ya había ocurrido en el Incremento 01 (LA-2026-042). | Ninguna — cerrado con `railway domain update ... --port 8080`, verificado con `/health` → 200. Lección: revisar siempre el log de arranque (`[server] Escuchando en el puerto ...`) contra el `targetPort` configurado después de cada primer deploy, sin asumir que coincide con `.env.example`. |
| DT-10 | Verificado y corregido: `BACKEND_API_URL`/`BACKEND_API_KEY` faltantes en el servicio Railway del frontend | Cerrado | frontend | El servicio `rutacar_landing_new` en Railway nunca tuvo configuradas `BACKEND_API_URL`/`BACKEND_API_KEY` — solo se habían seteado en el backend. El proxy devolvía 503 en cada envío real de producción. Detectado probando el flujo completo con Playwright contra la URL pública, no por ningún healthcheck individual (ambos servicios reportaban `SUCCESS` por separado). | Ninguna — variables sincronizadas entre ambos servicios, verificado con un envío real de punta a punta en producción (documento persistido + email de confirmación entregado). Ver LA-2026-047. |
| DT-11 | Verificado y corregido: `trust proxy` no configurado, rate limiting degradado a límite global | Cerrado | backend | `express-rate-limit` estaba activo pero sin `app.set("trust proxy", ...)` — detrás del proxy de Railway, esto producía `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR` en los logs de producción y hacía que el límite se aplicara por la IP del proxy, no por IP real del cliente. | Ninguna — cerrado con `app.set("trust proxy", 1)` en `src/app.ts`. Verificado localmente simulando una petición con `X-Forwarded-For` antes/después del fix, y confirmado en logs de producción tras el redeploy sin más ocurrencias del error. Ver LA-2026-048. |
| DT-12 | Reglas de calificación del formulario de precalificación provisionales | P0 | backend | `src/services/precalificacion.service.ts` usa una regla simple y genérica (`v0-simple-provisional`, plazo "inmediato"→prioritario / "1-3-meses"→a_seguir / resto→descartar), no las reglas reales del negocio. Mismo patrón que DT-01. | Depende de la misma reunión con el equipo comercial de Ruta Car que DT-01 (riesgo de negocio, no técnico — ver `plan-ejecucion.md` del Incremento 03). |

## Cómo se usa

Revisar al inicio de cada sprint (mismo hábito que el Incremento 01). Cerrar un ítem = mover su fila a "Cerrado" con el commit/fecha que lo resolvió, no borrarla.
